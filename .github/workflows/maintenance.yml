name: Repository Maintenance

on:
  push:
    paths:
      - labels.yml
    branches: [main]
  workflow_dispatch:

permissions: {}

jobs:
  sync-labels:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v7
      - name: Install github-label-sync
        # 固定版本保证可重现，避免 latest 漂移/供应链意外
        run: npm install -g @endbug/github-label-sync@3.0.0
      - name: Sync labels
        env:
          GITHUB_ACCESS_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          github-label-sync \
            --labels labels.yml \
            --allow-added-labels \
            ${{ github.repository }}
